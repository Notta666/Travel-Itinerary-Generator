# Python 3.10+ standard library is used for the core system.
# No external Python package dependencies are required to run the core pipeline.
#
# Optional development packages:
pytest>=7.0.0
black>=23.0.0
flake8>=6.0.0

# Web App dependencies
fastapi>=0.100.0
uvicorn>=0.23.0

# Brochure template rendering
jinja2>=3.0.0

# Xiaohongshu research (Playwright backend, replaces OpenCLI)
playwright>=1.40.0
